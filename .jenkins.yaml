---
environment_variables:
  NEXTCLOUD_VERSION: "33.0.0-dev-sunet1"
git:
  branch: main
triggers:
  cron: "@daily"
clean_workspace: true
builders:
  - docker
docker_name: drive/nextcloud-custom
docker_tags:
  - "${NEXTCLOUD_VERSION}"
  - "latest"
docker_no_cache: true
